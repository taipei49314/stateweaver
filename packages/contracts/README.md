# StateWeaver contracts

Immutable, versioned Pydantic contracts for actions, evidence, state, worlds, replay receipts,
Oracles, and event histories. These models define StateWeaver's fail-closed trust boundaries; an
unchecked in-memory model instance is never treated as an attestation.
