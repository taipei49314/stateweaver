# StateWeaver replay

Deterministic clean-root replay models, ports, and kernel. Replay binds typed action envelopes,
policy decisions, observations, Oracle results, action logs, and state fingerprints without
exposing arbitrary shell commands or external targets.
