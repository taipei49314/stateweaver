# Web design concepts

These images are static interface design references, not captured replay evidence or release
proofs. Browser-verified screenshots from the implemented local application live under
`output/playwright/stateweaver-final/` and are labeled separately from these concepts.
