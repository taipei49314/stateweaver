[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "stateweaver-adapter-docker-compose"
version = "0.1.0"
description = "Fail-closed Docker Compose adapter for StateWeaver's fixed synthetic demo."
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
dependencies = [
  "pydantic>=2.12,<3",
  "stateweaver-worlds==0.1.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/stateweaver"]

[tool.uv.sources]
stateweaver-worlds = { workspace = true }

[tool.pytest.ini_options]
addopts = "--strict-config --strict-markers"
asyncio_mode = "strict"
pythonpath = ["src", "../../../packages/worlds/src"]
testpaths = ["tests"]
